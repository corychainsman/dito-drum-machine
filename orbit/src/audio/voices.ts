// Stub for Phase 1 — voice synthesis implemented in Phase 3

export function triggerKick(
  _ctx: AudioContext, _time: number, _fader: number
): void {
  // Stub
}

export function triggerSnare(
  _ctx: AudioContext, _time: number, _fader: number
): void {
  // Stub
}

export function triggerHihat(
  _ctx: AudioContext, _time: number, _fader: number
): void {
  // Stub
}

export function triggerClap(
  _ctx: AudioContext, _time: number, _fader: number,
  _sampleBuffers: Map<string, AudioBuffer>
): void {
  // Stub
}

export function triggerTom(
  _ctx: AudioContext, _time: number, _fader: number,
  _sampleBuffers: Map<string, AudioBuffer>
): void {
  // Stub
}
